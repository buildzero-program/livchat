# fly.toml - AST (Agent Service Toolkit)
# https://fly.io/docs/reference/configuration/

app = 'livchat-ast'
primary_region = 'gru'

[build]
  dockerfile = 'docker/Dockerfile.service'

[env]
  MODE = 'prod'
  PORT = '9000'
  DATABASE_TYPE = 'postgres'
  TZ = 'America/Sao_Paulo'
  LANGSMITH_TRACING = 'true'
  LANGSMITH_PROJECT = 'livchat'
  LANGSMITH_ENDPOINT = 'https://api.smith.langchain.com'

[http_service]
  internal_port = 9000
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1

# Secrets to set via flyctl:
# flyctl secrets set \
#   AUTH_SECRET="..." \
#   OPENAI_API_KEY="..." \
#   GOOGLE_API_KEY="..." \
#   GROQ_API_KEY="..." \
#   LANGSMITH_API_KEY="..." \
#   POSTGRES_USER="neondb_owner" \
#   POSTGRES_PASSWORD="..." \
#   POSTGRES_HOST="ep-soft-pine-acc7zl9c-pooler.sa-east-1.aws.neon.tech" \
#   POSTGRES_PORT="5432" \
#   POSTGRES_DB="neondb"
